---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: linodenodeclasses.karpenter.k8s.linode
spec:
  group: karpenter.k8s.linode
  names:
    categories:
    - karpenter
    kind: LinodeNodeClass
    listKind: LinodeNodeClassList
    plural: linodenodeclasses
    shortNames:
    - lnc
    - lncs
    singular: linodenodeclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .spec.role
      name: Role
      priority: 1
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: LinodeNodeClass is the Schema for the LinodeNodeClass API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LinodeNodeClassSpec is the top level specification for the Linode Karpenter Provider.
              This will contain configuration necessary to launch instances in Linode.
            properties:
              authorizedKeys:
                description: authorizedKeys is a list of SSH public keys to add to
                  the instance.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              authorizedUsers:
                description: authorizedUsers is a list of usernames to add to the
                  instance.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              backupsEnabled:
                description: backupsEnabled is a boolean indicating whether backups
                  should be enabled for the instance.
                type: boolean
              configuration:
                description: |-
                  configuration is the Akamai instance configuration OS,
                  if not specified, this defaults to the default configuration associated to the instance.
                properties:
                  kernel:
                    description: kernel is a Kernel ID to boot a Linode with. (e.g
                      linode/latest-64bit).
                    type: string
                type: object
              dataDisks:
                description: |-
                  dataDisks is a map of any additional disks to add to an instance,
                  The sum of these disks + the OSDisk must not be more than allowed on a linodes plan
                properties:
                  sdb:
                    description: sdb is a disk for the instance.
                    properties:
                      diskID:
                        description: diskID is the linode assigned ID of the disk.
                        type: integer
                      filesystem:
                        description: filesystem of disk to provision, the default
                          disk filesystem is "ext4".
                        enum:
                        - raw
                        - swap
                        - ext3
                        - ext4
                        - initrd
                        type: string
                      label:
                        description: label for the instance disk, if nothing is provided,
                          it will match the device name.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: size of the disk in resource.Quantity notation.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                  sdc:
                    description: sdc is a disk for the instance.
                    properties:
                      diskID:
                        description: diskID is the linode assigned ID of the disk.
                        type: integer
                      filesystem:
                        description: filesystem of disk to provision, the default
                          disk filesystem is "ext4".
                        enum:
                        - raw
                        - swap
                        - ext3
                        - ext4
                        - initrd
                        type: string
                      label:
                        description: label for the instance disk, if nothing is provided,
                          it will match the device name.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: size of the disk in resource.Quantity notation.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                  sdd:
                    description: sdd is a disk for the instance.
                    properties:
                      diskID:
                        description: diskID is the linode assigned ID of the disk.
                        type: integer
                      filesystem:
                        description: filesystem of disk to provision, the default
                          disk filesystem is "ext4".
                        enum:
                        - raw
                        - swap
                        - ext3
                        - ext4
                        - initrd
                        type: string
                      label:
                        description: label for the instance disk, if nothing is provided,
                          it will match the device name.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: size of the disk in resource.Quantity notation.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                  sde:
                    description: sde is a disk for the instance.
                    properties:
                      diskID:
                        description: diskID is the linode assigned ID of the disk.
                        type: integer
                      filesystem:
                        description: filesystem of disk to provision, the default
                          disk filesystem is "ext4".
                        enum:
                        - raw
                        - swap
                        - ext3
                        - ext4
                        - initrd
                        type: string
                      label:
                        description: label for the instance disk, if nothing is provided,
                          it will match the device name.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: size of the disk in resource.Quantity notation.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                  sdf:
                    description: sdf is a disk for the instance.
                    properties:
                      diskID:
                        description: diskID is the linode assigned ID of the disk.
                        type: integer
                      filesystem:
                        description: filesystem of disk to provision, the default
                          disk filesystem is "ext4".
                        enum:
                        - raw
                        - swap
                        - ext3
                        - ext4
                        - initrd
                        type: string
                      label:
                        description: label for the instance disk, if nothing is provided,
                          it will match the device name.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: size of the disk in resource.Quantity notation.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                  sdg:
                    description: sdg is a disk for the instance.
                    properties:
                      diskID:
                        description: diskID is the linode assigned ID of the disk.
                        type: integer
                      filesystem:
                        description: filesystem of disk to provision, the default
                          disk filesystem is "ext4".
                        enum:
                        - raw
                        - swap
                        - ext3
                        - ext4
                        - initrd
                        type: string
                      label:
                        description: label for the instance disk, if nothing is provided,
                          it will match the device name.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: size of the disk in resource.Quantity notation.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                  sdh:
                    description: sdh is a disk for the instance.
                    properties:
                      diskID:
                        description: diskID is the linode assigned ID of the disk.
                        type: integer
                      filesystem:
                        description: filesystem of disk to provision, the default
                          disk filesystem is "ext4".
                        enum:
                        - raw
                        - swap
                        - ext3
                        - ext4
                        - initrd
                        type: string
                      label:
                        description: label for the instance disk, if nothing is provided,
                          it will match the device name.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: size of the disk in resource.Quantity notation.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                type: object
              diskEncryption:
                description: diskEncryption determines if the disks of the instance
                  should be encrypted. The default is disabled.
                enum:
                - enabled
                - disabled
                type: string
              firewallID:
                description: firewallID is the id of the cloud firewall to apply to
                  the Linode Instance
                type: integer
              image:
                description: image is the Linode image to use for the instance.
                type: string
              ipv6Options:
                description: |-
                  ipv6Options defines the IPv6 options for the instance.
                  If not specified, IPv6 ranges won't be allocated to instance.
                properties:
                  enableRanges:
                    description: |-
                      enableRanges is an option to enable IPv6 ranges for the instance.
                      If set to true, the instance will have a range of IPv6 addresses.
                      This is useful for instances that require multiple IPv6 addresses.
                      Defaults to false.
                    type: boolean
                    x-kubernetes-validations:
                    - message: Value is immutable
                      rule: self == oldSelf
                  enableSLAAC:
                    description: |-
                      enableSLAAC is an option to enable SLAAC (Stateless Address Autoconfiguration) for the instance.
                      This is useful for IPv6 addresses, allowing the instance to automatically configure its own IPv6 address.
                      Defaults to false.
                    type: boolean
                    x-kubernetes-validations:
                    - message: Value is immutable
                      rule: self == oldSelf
                  isPublicIPv6:
                    description: |-
                      isPublicIPv6 is an option to enable public IPv6 for the instance.
                      If set to true, the instance will have a publicly routable IPv6 range.
                      Defaults to false.
                    type: boolean
                    x-kubernetes-validations:
                    - message: Value is immutable
                      rule: self == oldSelf
                type: object
              kubelet:
                description: |-
                  Kubelet defines args to be used when configuring kubelet on provisioned nodes.
                  They are a subset of the upstream types, recognizing not all options may be supported.
                  Wherever possible, the types and names should reflect the upstream kubelet types.
                properties:
                  clusterDNS:
                    description: |-
                      clusterDNS is a list of IP addresses for the cluster DNS server.
                      Note that not all providers may use all addresses.
                    items:
                      type: string
                    type: array
                  cpuCFSQuota:
                    description: CPUCFSQuota enables CPU CFS quota enforcement for
                      containers that specify CPU limits.
                    type: boolean
                  evictionHard:
                    additionalProperties:
                      type: string
                    description: EvictionHard is the map of signal names to quantities
                      that define hard eviction thresholds
                    type: object
                    x-kubernetes-validations:
                    - message: valid keys for evictionHard are ['memory.available','nodefs.available','nodefs.inodesFree','imagefs.available','imagefs.inodesFree','pid.available']
                      rule: self.all(x, x in ['memory.available','nodefs.available','nodefs.inodesFree','imagefs.available','imagefs.inodesFree','pid.available'])
                  evictionMaxPodGracePeriod:
                    description: |-
                      EvictionMaxPodGracePeriod is the maximum allowed grace period (in seconds) to use when terminating pods in
                      response to soft eviction thresholds being met.
                    format: int32
                    type: integer
                  evictionSoft:
                    additionalProperties:
                      type: string
                    description: EvictionSoft is the map of signal names to quantities
                      that define soft eviction thresholds
                    type: object
                    x-kubernetes-validations:
                    - message: valid keys for evictionSoft are ['memory.available','nodefs.available','nodefs.inodesFree','imagefs.available','imagefs.inodesFree','pid.available']
                      rule: self.all(x, x in ['memory.available','nodefs.available','nodefs.inodesFree','imagefs.available','imagefs.inodesFree','pid.available'])
                  evictionSoftGracePeriod:
                    additionalProperties:
                      type: string
                    description: EvictionSoftGracePeriod is the map of signal names
                      to quantities that define grace periods for each eviction signal
                    type: object
                    x-kubernetes-validations:
                    - message: valid keys for evictionSoftGracePeriod are ['memory.available','nodefs.available','nodefs.inodesFree','imagefs.available','imagefs.inodesFree','pid.available']
                      rule: self.all(x, x in ['memory.available','nodefs.available','nodefs.inodesFree','imagefs.available','imagefs.inodesFree','pid.available'])
                  kubeReserved:
                    additionalProperties:
                      type: string
                    description: KubeReserved contains resources reserved for Kubernetes
                      system components.
                    type: object
                    x-kubernetes-validations:
                    - message: valid keys for kubeReserved are ['cpu','memory','ephemeral-storage','pid']
                      rule: self.all(x, x=='cpu' || x=='memory' || x=='ephemeral-storage'
                        || x=='pid')
                    - message: kubeReserved value cannot be a negative resource quantity
                      rule: self.all(x, !self[x].startsWith('-'))
                  maxPods:
                    description: |-
                      MaxPods is an override for the maximum number of pods that can run on
                      a worker node instance.
                    format: int32
                    minimum: 0
                    type: integer
                  podsPerCore:
                    description: |-
                      PodsPerCore is an override for the number of pods that can run on a worker node
                      instance based on the number of cpu cores. This value cannot exceed MaxPods, so, if
                      MaxPods is a lower value, that value will be used.
                    format: int32
                    minimum: 0
                    type: integer
                  systemReserved:
                    additionalProperties:
                      type: string
                    description: SystemReserved contains resources reserved for OS
                      system daemons and kernel memory.
                    type: object
                    x-kubernetes-validations:
                    - message: valid keys for systemReserved are ['cpu','memory','ephemeral-storage','pid']
                      rule: self.all(x, x=='cpu' || x=='memory' || x=='ephemeral-storage'
                        || x=='pid')
                    - message: systemReserved value cannot be a negative resource
                        quantity
                      rule: self.all(x, !self[x].startsWith('-'))
                type: object
                x-kubernetes-validations:
                - message: evictionSoft OwnerKey does not have a matching evictionSoftGracePeriod
                  rule: has(self.evictionSoft) ? self.evictionSoft.all(e, (e in self.evictionSoftGracePeriod)):true
                - message: evictionSoftGracePeriod OwnerKey does not have a matching
                    evictionSoft
                  rule: has(self.evictionSoftGracePeriod) ? self.evictionSoftGracePeriod.all(e,
                    (e in self.evictionSoft)):true
              labels:
                additionalProperties:
                  type: string
                description: labels is a map of Kubernetes node labels to apply to
                  the Node.
                type: object
              linodeInterfaces:
                description: linodeInterfaces is a list of Linode network interfaces
                  to use for the instance. Requires Linode Interfaces beta opt-in
                  to use.
                items:
                  description: LinodeInterfaceCreateOptions defines the linode network
                    interface config
                  properties:
                    defaultRoute:
                      description: defaultRoute is the default route for the interface.
                      properties:
                        ipv4:
                          description: ipv4 is the IPv4 default route for the interface.
                          type: boolean
                        ipv6:
                          description: ipv6 is the IPv6 default route for the interface.
                          type: boolean
                      type: object
                    firewallID:
                      description: firewallID is the ID of the firewall to use for
                        the interface.
                      type: integer
                    public:
                      description: public is the public interface configuration for
                        the interface.
                      properties:
                        ipv4:
                          description: ipv4 is the IPv4 configuration for the public
                            interface.
                          properties:
                            addresses:
                              description: addresses is the IPv4 addresses for the
                                public interface.
                              items:
                                description: PublicInterfaceIPv4AddressCreateOptions
                                  defines the public IPv4 address and whether it is
                                  primary
                                properties:
                                  address:
                                    description: address is the IPv4 address for the
                                      public interface.
                                    minLength: 1
                                    type: string
                                  primary:
                                    description: primary is a boolean indicating whether
                                      the address is primary.
                                    type: boolean
                                required:
                                - address
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - address
                              x-kubernetes-list-type: map
                          type: object
                        ipv6:
                          description: ipv6 is the IPv6 configuration for the public
                            interface.
                          properties:
                            ranges:
                              description: ranges is the IPv6 ranges for the public
                                interface.
                              items:
                                description: PublicInterfaceIPv6RangeCreateOptions
                                  defines the IPv6 range for a public interface
                                properties:
                                  range:
                                    description: range is the IPv6 range for the public
                                      interface.
                                    minLength: 1
                                    type: string
                                required:
                                - range
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - range
                              x-kubernetes-list-type: map
                          type: object
                      type: object
                    vlan:
                      description: vlan is the VLAN interface configuration for the
                        interface.
                      properties:
                        ipamAddress:
                          description: ipamAddress is the IP address to assign to
                            the interface.
                          type: string
                        vlanLabel:
                          description: vlanLabel is the label of the VLAN.
                          minLength: 1
                          type: string
                      required:
                      - vlanLabel
                      type: object
                    vpc:
                      description: vpc is the VPC interface configuration for the
                        interface.
                      properties:
                        ipv4:
                          description: ipv4 is the IPv4 configuration for the interface.
                          properties:
                            addresses:
                              description: addresses is the IPv4 addresses for the
                                interface.
                              items:
                                description: VPCInterfaceIPv4AddressCreateOptions
                                  defines the IPv4 configuration for a VPC interface
                                properties:
                                  address:
                                    description: address is the IPv4 address for the
                                      interface.
                                    minLength: 1
                                    type: string
                                  nat1to1Address:
                                    description: nat1to1Address is the NAT 1:1 address
                                      for the interface.
                                    type: string
                                  primary:
                                    description: primary is a boolean indicating whether
                                      the address is primary.
                                    type: boolean
                                required:
                                - address
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - address
                              x-kubernetes-list-type: map
                            ranges:
                              description: ranges is the IPv4 ranges for the interface.
                              items:
                                description: VPCInterfaceIPv4RangeCreateOptions defines
                                  the IPv4 range for a VPC interface
                                properties:
                                  range:
                                    description: range is the IPv4 range for the interface.
                                    minLength: 1
                                    type: string
                                required:
                                - range
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - range
                              x-kubernetes-list-type: map
                          type: object
                        ipv6:
                          description: ipv6 is the IPv6 configuration for the interface.
                          properties:
                            isPublic:
                              description: isPublic is a boolean indicating whether
                                the interface is public.
                              type: boolean
                            ranges:
                              description: ranges is the IPv6 ranges for the interface.
                              items:
                                description: VPCInterfaceIPv6RangeCreateOptions defines
                                  the IPv6 range for a VPC interface
                                properties:
                                  range:
                                    description: range is the IPv6 range for the interface.
                                    minLength: 1
                                    type: string
                                required:
                                - range
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - range
                              x-kubernetes-list-type: map
                            slaac:
                              description: slaac is the IPv6 SLAAC configuration for
                                the interface.
                              items:
                                description: VPCInterfaceIPv6SLAACCreateOptions defines
                                  the Range for IPv6 SLAAC
                                properties:
                                  range:
                                    description: range is the IPv6 range for the interface.
                                    minLength: 1
                                    type: string
                                required:
                                - range
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - range
                              x-kubernetes-list-type: map
                          required:
                          - isPublic
                          type: object
                        subnetId:
                          description: subnetId is the ID of the subnet to use for
                            the interface.
                          type: integer
                      type: object
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              lkeK8sVersion:
                description: |-
                  lkeK8sVersion is the Kubernetes version for LKE node pools.
                  Only used when managedLKE is true. Only available for Enterprise LKE clusters.
                type: string
              lkeUpdateStrategy:
                description: |-
                  lkeUpdateStrategy defines how nodes are updated when the LKE node pool is modified.
                  Only used when managedLKE is true.
                  Valid values are "rolling_update" and "on_recycle".
                enum:
                - rolling_update
                - on_recycle
                type: string
              managedLKE:
                description: |-
                  managedLKE enables LKE-managed node pool mode. When true, Karpenter provisions
                  nodes via LKE NodePool APIs instead of creating direct Linode instances.
                  In this mode, LKE manages the node image, network configuration, and kubelet.
                  Fields like image, authorizedKeys, linodeInterfaces, osDisk, dataDisks,
                  placementGroup, configuration, vpcID, ipv6Options, swapSize, and kubelet are ignored.
                type: boolean
              osDisk:
                description: |-
                  osDisk is a configuration for the root disk that includes the OS,
                  if not specified, this defaults to whatever space is not taken up by the DataDisks
                properties:
                  diskID:
                    description: diskID is the linode assigned ID of the disk.
                    type: integer
                  filesystem:
                    description: filesystem of disk to provision, the default disk
                      filesystem is "ext4".
                    enum:
                    - raw
                    - swap
                    - ext3
                    - ext4
                    - initrd
                    type: string
                  label:
                    description: label for the instance disk, if nothing is provided,
                      it will match the device name.
                    type: string
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: size of the disk in resource.Quantity notation.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                required:
                - size
                type: object
              placementGroup:
                description: placementGroup makes the linode to be launched in that
                  specific group.
                properties:
                  compliant_only:
                    description: CompliantOnly indicates whether all Linodes in this
                      group should be compliant with the group's placement group type
                    type: boolean
                  id:
                    description: ID is the Linode Placement Group ID to launch the
                      instance in.
                    type: integer
                required:
                - id
                type: object
              swap_size:
                description: swapSize is the size of the swap disk in MiB.
                type: integer
              tags:
                description: tags is a list of tags to apply to the Linode instance.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              vpcID:
                description: vpcID is the ID of an existing VPC in Linode.
                type: integer
            type: object
          status:
            description: LinodeNodeClassStatus contains the resolved state of the
              LinodeNodeClass
            properties:
              conditions:
                description: Conditions contains signals for health and readiness
                items:
                  description: Condition aliases the upstream type and adds additional
                    helper methods
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              linodeImages:
                description: |-
                  LinodeImage contains the current LinodeImage values that are available to the
                  cluster under the LinodeImage selectors.
                items:
                  description: LinodeImage contains resolved LinodeImage selector
                    values utilized for node launch
                  properties:
                    deprecated:
                      description: Deprecation status of the Image
                      type: boolean
                    id:
                      description: ID of the Image
                      type: string
                    label:
                      description: Label of the Image
                      type: string
                    requirements:
                      description: Requirements used to select the Image
                      items:
                        description: |-
                          A node selector requirement is a selector that contains values, a key, and an operator
                          that relates the key and values.
                        properties:
                          key:
                            description: The label key that the selector applies to.
                            type: string
                          operator:
                            description: |-
                              Represents a key's relationship to a set of values.
                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                            type: string
                          values:
                            description: |-
                              An array of string values. If the operator is In or NotIn,
                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                              the values array must be empty. If the operator is Gt or Lt, the values
                              array must have a single element, which will be interpreted as an integer.
                              This array is replaced during a strategic merge patch.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - key
                        - operator
                        type: object
                      type: array
                  required:
                  - id
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
