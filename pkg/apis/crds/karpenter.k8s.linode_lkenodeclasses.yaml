---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: lkenodeclasses.karpenter.k8s.linode
spec:
  group: karpenter.k8s.linode
  names:
    categories:
    - karpenter
    kind: LKENodeClass
    listKind: LKENodeClassList
    plural: lkenodeclasses
    shortNames:
    - lkenc
    - lkencs
    singular: lkenodeclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: LKENodeClass is the Schema for the LKENodeClass API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LKENodeClassSpec defines the desired state for LKE-managed node pools.
              This is a simplified spec compared to LinodeNodeClass since LKE manages
              most instance configuration (image, network interfaces, placement, etc.).
            properties:
              diskEncryption:
                description: |-
                  diskEncryption determines if the disks of the instance should be encrypted.
                  This is only available for Enterprise LKE clusters.
                enum:
                - enabled
                - disabled
                type: string
              firewallID:
                description: firewallID is the ID of the cloud firewall to apply to
                  the node pool.
                type: integer
              k8sVersion:
                description: |-
                  k8sVersion is the Kubernetes version for the node pool.
                  This is only available for Enterprise LKE clusters.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: labels is a map of Kubernetes node labels to apply to
                  the Node.
                type: object
              tags:
                description: tags is a list of tags to apply to the LKE node pool.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              updateStrategy:
                description: |-
                  updateStrategy defines how nodes are updated when the node pool is modified.
                  Valid values are "rolling_update" and "on_recycle".
                enum:
                - rolling_update
                - on_recycle
                type: string
            type: object
          status:
            description: LKENodeClassStatus contains the resolved state of the LKENodeClass
            properties:
              conditions:
                description: Conditions contains signals for health and readiness
                items:
                  description: Condition aliases the upstream type and adds additional
                    helper methods
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
